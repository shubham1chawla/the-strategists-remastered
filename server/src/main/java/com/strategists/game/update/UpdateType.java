package com.strategists.game.update;

public enum UpdateType {
	BANKRUPTCY, BONUS, CHEAT, END, EVENT, INVEST, INVITE, JAIL, JOIN, KICK, MOVE, PING, PREDICTION, RENT, RESET, START,
	TRADE, TREND, TURN;
}
